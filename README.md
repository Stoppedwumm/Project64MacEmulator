# Project64MacEmulator
(Emulating an emulator (what the heck))

## Installation
- Download the repo
- Open the terminal in the folder
- Run `sh install.sh` (**READ THE NEXT INSTRUCTIONS CAREFULLY**)
- Use the default path and don't create an icon
- At launch, use Video64
- To relaunch, run `sh start.sh`
